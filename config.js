const CONFIG = {
  SADNESS_THRESHOLD_HOURS: 4,   // hours with no one present before pet gets sad
  POLL_INTERVAL_MS: 10000,       // how often to check the API and feed (ms)
  MEMBER_COUNT: 13,              // number of lab members
  GROWTH_PER_TICK: 1,            // base multiplier for growth points per tick
  POINTS_PER_LEVEL: 1000,        // points needed to advance one level
  MAX_LEVEL: 10,                 // maximum level
};
